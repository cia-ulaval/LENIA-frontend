import { StyleSheet } from "react-native";

export const appStyle = StyleSheet.create({
  container: {
    width: "100%",
    backgroundColor: "green",
    display: "flex",
    flexDirection: "row",
    
  },
});
