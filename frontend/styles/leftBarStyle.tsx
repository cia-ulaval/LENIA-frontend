

import { StyleSheet } from "react-native";

export const leftBarStyle = StyleSheet.create({
    container:{
        width: "75%",

        backgroundColor: "yellow",
        alignItems: "center"
    }
});